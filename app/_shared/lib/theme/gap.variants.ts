import { tv } from 'tailwind-variants'

export const gapVariants = tv({
  variants: {
    gap: {
      xs: 'gap-0.5',
      sm: 'gap-1.5',
      md: 'gap-3',
      lg: 'gap-5',
      xl: 'gap-7',
    },
  },
})
